{% extends 'base.html.twig' %}

{% block title %}Editer manga
{% endblock %}

{% block body %}

	<div class="container flex-collum desk-flex-collum">

		<h1>Editer manga</h1>

		{{ include('manga/_form.html.twig', {'button_label': 'Editer'}) }}

		<a href="{{ path('manga_index') }}">
			<button class="btn btn-primary">
				Retourner à la liste</button>
		</a>

		{{ include('manga/_delete_form.html.twig') }}
	</div>
{% endblock %}
