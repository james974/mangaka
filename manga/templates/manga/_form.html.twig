{{ form_start(form, { 'attr': {'class': 'form-control input'}}) }}

<div class="form-group">
	{{ form_row(form.nom_manga, { 'attr': {'class': 'form-control input'}})}}
</div>
<div class="form-group">
	{{ form_row(form.tome, { 'attr': {'class': 'form-control input'}})}}
</div>
<div class="form-group">

	<div class="select">

		{{ form_row(form.magasin, { 'attr': {'id': 'standard-select'}})}}
		<span class="focus"></span>
	</div>

</div>
<div class="form-group">
    <div class="select">
        {{ form_row(form.auteur, { 'attr': {'class': 'form-control input'}})}}
    </div>
</div>
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
