<form method="post" action="{{ path('manga_delete', {'id': manga.id}) }}" onsubmit="return confirm('Vous etes sur de vouloir supprimer ? ');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ manga.id) }}">
    <button class="btn">Supprimer</button>
</form>
